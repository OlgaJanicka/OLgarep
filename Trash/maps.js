//function maps(x) {
    var array = [1, 2, 3]
    var newArray = [];
    for (var i = 0; i < array.length; i++) {
        newArray[i] = array[i] * 2
        newArray.push(array[i] * 2)
        console.log(newArray ); 
}
console.log(newArray );


