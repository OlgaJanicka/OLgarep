function distinct(a) {
    let array;
    let cleanArray = [];
    let arrayElement;
    
    for (let arrayElement = 0;arrayElement <= array.length; arrayElement++){
    let valueToCheck = array[arrayElement];
    }
}

